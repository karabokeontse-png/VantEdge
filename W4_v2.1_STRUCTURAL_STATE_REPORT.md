# W4 v2.1 Structural State Report

**Scope:** READ BEFORE WRITE v1.2 — Scope Locked Execution Pack  
**Date:** 2026-07-01  
**Constraint:** Observation-only. Zero inference. Zero recommendations. Zero proposals. No fixes.  
**W4 v2.1 Invariants embedded per §1 of W4 Execution Contract v2.1**

---

## Inspection Scope

| Layer | Files |
|---|---|
| Primary Targets (8) | ConfidenceEngine.kt, ExtractionDecisionCoordinator.kt, JsonExtractionEngine.kt, SemanticValidator.kt, CompatibilityEngine.kt, CompatibilityResult.kt, JsonRepairUtil.kt, PipelineTrace.kt |
| High-Risk Confidence Paths (4) | ProfileExtractionEngine.kt, RuleBasedEmergencyExtractor.kt, DocumentPreprocessor.kt, JobExtractionOrchestrator.kt |
| AI Gateway Controlled Observation (4) | AiGateway.kt, GeminiService.kt, AiRequest.kt, LlmJobExtractor.kt |
| Compatibility Domain | CompatibilityStore.kt |

**Exclusions (W3 frozen, §3):** Retry logic, timeout systems, circuit breaker systems, async callback orchestration, thread scheduling logic, network transport reliability layers.

---

## W4 v2.1 Invariant Reference

| # | Invariant | Clause |
|---|---|---|
| 1.1 | Deterministic Extraction Boundary — stable field structure, consistent traversal, no unordered iteration side-effects | §1.1 |
| 1.2 | Schema Integrity Boundary — parsed AI outputs conform to validated schemas before downstream usage | §1.2 |
| 1.3 | Deterministic Inference Parameters — deterministic-compatible settings for analytical pipelines (excludes generative) | §1.3 |
| 1.4 | Reasoning Content Containment Boundary — non-final reasoning artifacts not propagate into structured outputs | §1.4 |
| 1.5 | Confidence Integrity Boundary — derived signals only; not directly control flow without normalization; deterministic adjustment | §1.5 |
| 1.6 | Compatibility Determinism Boundary — deterministic function over normalized inputs | §1.6 |
| 1.7 | Hallucination Containment Boundary — external references unverified by default; verification state as property | §1.7 |
| 1.8 | Observability Integrity Boundary — traceability without execution interference | §1.8 |

---

## File-by-File Structural State Report

### PRIMARY INSPECTION TARGETS

---

#### 1. ConfidenceEngine.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/ConfidenceEngine.kt`  
**Role:** Field-level confidence computation from LLM confidence map, semantic adjustments, and schema failures.

| Property | Observation |
|---|---|
| Symbols | `ConfidenceEngine` (object), `ConfidenceResult` (data class) |
| Entry points | `compute(llmConfidenceMap, semanticAdjustments, schemaFailures, source, correlationId)` |
| Dependencies | `PipelineTrace`, `FieldConfidence` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | **VIOLATION** | L31: `(llmConfidenceMap.keys + semanticAdjustments.keys + schemaFailures).toSet()` — `toSet()` on combined iterables produces non-deterministic iteration order when source maps have insertion-order-dependent iterables. L29: `mutableMapOf<String, FieldConfidence>()` — insertion-order dependent map. L60: `fieldConfidenceMap.entries.joinToString(", ")` — output ordering depends on map entry iteration order. The `fieldConfidenceMap` returned to callers is a `MutableMap` with only insertion-order guarantees, not a deterministic-iteration contract. |
| 1.5 | OBSERVED | L34: `val llmConf = llmConfidenceMap[field] ?: 0.3f` — deterministic fallback. L41: `overall = (llmConf * 0.5f + validationConf * 0.3f + consistencyConf * 0.2f).coerceIn(0.0f, 1.0f)` — fixed weights, deterministic linear combination. Values are derived signals, not authoritative truth. No flow-control bypass of the adjustment layer. |
| 1.8 | PRESENT | L21-L27: `PipelineTrace.entry` with field sizes, method, correlationId. L64-L71: `PipelineTrace.exit` with overall, fields count, method, field details. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **PRESENT** | L31 `toSet()`, L29 `mutableMapOf`, L60 `entries.joinToString` |
| hardcoded confidence | **PRESENT** | L34: 0.3f default, L41: 0.5f/0.3f/0.2f weights |

---

#### 2. ExtractionDecisionCoordinator.kt

**Path:** `app/src/main/java/com/vantedge/app/domain/extraction/ExtractionDecisionCoordinator.kt`  
**Role:** Decision routing based on field confidence thresholds (Accept / AcceptWithWarning / UserReview / Failure).

| Property | Observation |
|---|---|
| Symbols | `ExtractionDecisionCoordinator` (object) |
| Entry points | `decide(fieldConfidenceMap, source, correlationId)` |
| Dependencies | `PipelineTrace`, `FieldConfidence`, `ExtractionDecision` (sealed interface) |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | **VIOLATION** | L35: `fieldConfidenceMap.filter { (_, conf) -> conf.overallConfidence < ACCEPT_THRESHOLD }` — entry iteration order over the input `Map`. If callers pass mutable maps without guaranteed iteration contract, the `lowConfidenceFields` set and downstream decisions that enumerate fields (e.g., L65, L90) will have non-deterministic ordering. |
| 1.5 | OBSERVED | L8-L10: Thresholds are compile-time constants (0.90f, 0.70f, 0.50f). Decision logic is a deterministic function of the input `fieldConfidenceMap`. Values do not directly control execution flow without passing through thresholds. |
| 1.8 | PRESENT | L18-L22: `PipelineTrace.entry` with fields, method, correlationId. L26-L31/L41-L46/L66-L72/L80-L87/L99-L106: `PipelineTrace.exit` per decision path with decision, reason, details. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **PRESENT** | L35 `.filter` over Map entries, L65/L90 `.joinToString` over collections built from map iteration |
| hardcoded confidence | **PRESENT** | L8-L10: 0.90f, 0.70f, 0.50f constants |

---

#### 3. JsonExtractionEngine.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/JsonExtractionEngine.kt`  
**Role:** Extracts valid JSON from raw AI response strings using multi-strategy parsing.

| Property | Observation |
|---|---|
| Symbols | `JsonExtractionEngine` (object), `ExtractionResult` (data class) |
| Entry points | `extract(raw: String)` |
| Dependencies | `PipelineTrace`, `JSONObject` |
| Strategies (in order) | direct_parse → markdown_strip → nesting_aware_root → substring_scan → failed |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | L31: `extractInternal(normalized)` — deterministic character-level scanning. L74-L76: `candidates.sortedByDescending { it.length }` — stable sort produces deterministic order (no equal-length tie-breaker ambiguity as sort is stable and candidates are produced in document order by `findAllNestingAwareCandidates`). L87-L123: `extractNestingAwareRoot` — deterministic brace-matching scan. L125-L163: `findAllNestingAwareCandidates` — deterministic brace-matching scan. No unordered iteration. |
| 1.4 | **GAP** | L53-L57: Markdown fence stripping (` ```json`, ` ``` `, `**...**`, `*...*`). **Only markdown formatting tokens are removed.** No guard exists for: chain-of-thought reasoning text preceding/following JSON, XML-style reasoning tags (e.g., `<thinking>...</thinking>`), or any non-markdown reasoning artifact. If the model emits CoT text before the JSON block without a markdown fence, the text passes through to `extractInternal` and causes strategy fallthrough or failure. The system assumes markdown fencing is the only contamination vector. |
| 1.8 | PRESENT | L22-L29: `PipelineTrace.dataQuality(EXTRACTION_START)` with lengths. L33-L42: `PipelineTrace.dataQuality(EXTRACTION_RESULT)` with strategy, success, failure reason, content length. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **ABSENT** | All iterations are deterministic character scans or stable sorts |
| reasoning token leakage | **GAP** | Markdown fences only — CoT, XML, unstructured text between AI response preamble and JSON payload not handled |

---

#### 4. SemanticValidator.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/SemanticValidator.kt`  
**Role:** Validates job extraction output semantics; produces confidence adjustments and warnings.

| Property | Observation |
|---|---|
| Symbols | `SemanticValidator` (object), `SemanticValidationResult` (data class) |
| Entry points | `validate(jobTitle, company, description, source, correlationId)` |
| Dependencies | `PipelineTrace`, `FieldConfidence`, `ExtractionMethod` |
| Blacklists | `HEADER_BLACKLIST` (5), `SECTION_HEADER_BLACKLIST` (10), `KNOWN_INVALID_TOKENS` (10) |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | **VIOLATION** | L48: `val adjustments = mutableMapOf<String, Float>()` — insertion-order dependent map. L102: `adjustments.entries.joinToString(", ")` — entry ordering depends on map iteration order. The `fieldConfidenceAdjustments` map returned to `ConfidenceEngine` has only insertion-order guarantees. |
| 1.5 | OBSERVED | Adjustments are hardcoded constants (-0.4f, -0.3f, -0.5f, -0.2f, +0.1f, -0.3f). Transformation layer is deterministic. `KNOWN_INVALID_TOKENS` blacklist (L26-L30) for hallucination marker detection scans for first-person hedging language. |
| 1.8 | PRESENT | L40-L46: `PipelineTrace.entry` with fields, method, correlationId. L104-L111: `PipelineTrace.exit` with passed, warnings, adjustments. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **PRESENT** | L48 `mutableMapOf`, L102 `entries.joinToString` |
| hardcoded confidence | **PRESENT** | Fixed adjustment constants at L55, L64, L71, L76, L87, L94-L95 |

---

#### 5. CompatibilityEngine.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/CompatibilityEngine.kt`  
**Role:** Parses AI-generated compatibility analysis (JsonNode) into structured `CompatibilityRecord`.

| Property | Observation |
|---|---|
| Symbols | `CompatibilityEngine` (class), `CompatibilityResult` (sealed class), `CompatibilityRecord`, `CourseRecommendation`, `GapItem`, `ProfileStats`, `QualificationRatio`, `RelevancyItem` |
| Entry points | `analyze(node: JsonNode, jobTitle, company, jobDescription)` |
| Dependencies | Jackson `JsonNode`, model classes |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.2 | **VIOLATION** | L39-L48: `node.get("profileStats")` — if `profileStats` is missing, `MissingNode.path(...)` returns `MissingNode` which silently provides defaults (`asInt(0)`, `asText("")`). No schema pre-validation of the `JsonNode` structure before field extraction. Malformed AI output produces a `CompatibilityRecord` with all zero/null default values without error. Same pattern at L50-L59 (qualificationRatio), L62-L75 (relevancyItems), L77-L110 (gaps), L112-L128 (top-level fields). No validation that required keys exist or have correct types. |
| 1.6 | OBSERVED | L23-L30: `analyze()` is a pure function of input `(node, jobTitle, company, jobDescription)`. `parseCompatibilityRecord` has no side effects. L61: `mutableListOf<RelevancyItem>()`, L77: `mutableListOf<GapItem>()` — insertion order from JSON array traversal (`for (itemNode in relevancyArray)` at L64), which is deterministic (JSON preserves array order). |
| 1.7 | **VIOLATION** | L86-L91: `CourseRecommendation` constructed at L85-L96 with `url = c.path("url").asText("")` and `provider = c.path("provider").asText("")` — ingested directly from AI output with **no verification state**. The `CourseRecommendation` data class (`data/model/CourseRecommendation.kt`) has 8 fields but **no `isVerified` property**. All external URLs and providers are treated as trusted by default. No validation, no verification flag, no origin tracking. |
| 1.8 | ABSENT | No `PipelineTrace` calls in this file. Observability enters through `ContractValidator` upstream (via `ContractValidator.validate`). |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **ABSENT** | JSON array iteration is ordered |
| unvalidated external URLs | **PRESENT** | L88-L89: `url` and `provider` ingested without verification |

---

#### 6. CompatibilityResult.kt

**Path:** `app/src/main/java/com/vantedge/app/data/model/CompatibilityResult.kt`  
**Role:** Data model for compatibility analysis results.

| Property | Observation |
|---|---|
| Symbols | `CompatibilityResult` (data class) |
| Fields | `score`, `matchedSkills`, `missingSkills`, `weakSections`, `suggestions`, `courses: List<CourseRecommendation>` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.7 | OBSERVED | Passive data class. Carries `courses: List<CourseRecommendation>` which contains unverified `url`/`provider` fields from upstream. No `isVerified` field on the model. This is a data propagation issue — the model itself is passive. The upstream gap is in `CompatibilityEngine.parseCompatibilityRecord`. |

---

#### 7. JsonRepairUtil.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/JsonRepairUtil.kt`  
**Role:** Repairs malformed JSON (unclosed braces, trailing commas) for downstream parsing.

| Property | Observation |
|---|---|
| Symbols | `JsonRepairUtil` (object), `RepairResult` (data class) |
| Entry points | `repair(raw: String)` |
| Dependencies | `JSONObject`, `JSONArray` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | L12-L47: Stack-based character-level brace matching. L17: `raw.forEach { c -> ... }` over String — deterministic (String iteration is ordered). L72-L116: `removeTrailingCommas` — deterministic character-level scanning. No unordered iteration. |
| 1.4 | OBSERVED | Only closes unclosed braces/brackets (L42-L47). Does not strip pre/post-JSON text. This is a structural repair utility, not a reasoning-token guard. Inherits the gap from callers who do not strip reasoning artifacts before calling `repair()`. |

**Presence of Risk Indicators:**

| Risk Indicator | Status |
|---|---|
| unordered iteration | **ABSENT** |

---

#### 8. PipelineTrace.kt

**Path:** `app/src/main/java/com/vantedge/app/domain/PipelineTrace.kt`  
**Role:** Observability facade — entry/exit logging, data quality events, validation guards.

| Property | Observation |
|---|---|
| Symbols | `PipelineTrace` (object) |
| Methods | `entry`, `exit`, `error`, `warn`, `dataQuality`, `validateScore`, `validateNonBlank`, `validateEnum` |
| Dependencies | `LogSink`, `android.util.Log` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.8 (ordering) | OBSERVED | L10: `context.entries.joinToString(" ")` — iteration over `Map.entries`. Callers use `mapOf` which produces `LinkedHashMap` — insertion-order is deterministic in practice. However, the contract type (`Map<String, Any?>`) does not enforce ordered iteration. Same pattern at L17 and L53. If a caller passes a non-deterministic-ordered Map implementation, log output ordering varies. |
| 1.8 (interference) | OBSERVED | L58-L86: `validateScore`, `validateNonBlank`, `validateEnum` are read-only observers — they call `warn` which logs but does not mutate business state. No observable path where PipelineTrace alters execution flow. |
| 1.8 (log determinism) | OBSERVED | All methods write synchronized `LogSink.write` calls. No threading guards on format strings — but `joinToString` is deterministic per call. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **AMBIGUOUS** | L10/L17/L53: `entries.joinToString` depends on map implementation. Callers contractually use `mapOf` (LinkedHashMap), so order is deterministic in practice. |

---

### HIGH-RISK CONFIDENCE PATHS

---

#### 9. ProfileExtractionEngine.kt — parse()

**Path:** `app/src/main/java/com/vantedge/app/data/engine/ProfileExtractionEngine.kt` (lines 642-743)  
**Role:** Parses normalized AI response JSON into `StructuredProfileExtraction` with hardcoded confidence values.

| Property | Observation |
|---|---|
| Symbol | `ProfileExtractionEngine.parse(json: JSONObject)` — private method |
| Produces | `StructuredProfileExtraction` with `ExtractedField`, `ExtractedExperience`, `ExtractedEducation`, `ExtractedCertification` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | L649: `for (i in 0 until (skillsArray?.length() ?: 0))` — indexed iteration over JSONArray (deterministic). Same pattern at L657, L679, L699. |
| 1.5 | **VIOLATION** | **Every constructed `ExtractedField` uses hardcoded `0.8f` confidence**: L652 (`skills`), L664 (`jobTitle`), L665 (`company`), L666 (`startDate`), L668 (`endDate`), L670 (`description`), L686 (`institution`), L687 (`qualification`), L688 (`fieldOfStudy`), L690 (`graduationYear`), L705 (`name`), L706 (`issuer`), L722 (`name`), L724 (`email`), L726 (`phone`), L730 (`headline`), L733 (`summary`). **All seventeen field construction sites are hardcoded to 0.8f**, regardless of AI model output quality, confidence, or any signal from the response. L714-L718: overall confidence derived from field presence/absence alone (0.85f/0.55f/0.2f), completely decoupled from per-field confidence. The `ConfidenceEngine` is never called for profile extraction. |
| 1.8 | OBSERVED | No `PipelineTrace` calls within `parse()`. Observability exists upstream in `callAi()`/`structureProfile()`. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| hardcoded confidence | **PRESENT** | 0.8f at all 17 `ExtractedField` construction sites (L652-L733) |

---

#### 10. RuleBasedEmergencyExtractor.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/RuleBasedEmergencyExtractor.kt`  
**Role:** Rule-based fallback for job extraction when AI gateway is unavailable.

| Property | Observation |
|---|---|
| Symbols | `RuleBasedEmergencyExtractor` (object) |
| Entry points | `extractJob(rawText, sourceType, correlationId)` |
| Dependencies | `PipelineTrace`, `ExtractionValidator`, `JobExtractionResult`, `ConfidenceBreakdown` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | L52: `lines.withIndex()` over `List<String>` — deterministic (List iteration is ordered). L63: `HEADER_BLACKLIST.any` — deterministic (List.any is ordered). L38-L41: `Regex` patterns are deterministic. L186-L193: `runGate0` — ordered signal list iteration. |
| 1.5 | OBSERVED | L139: `confidenceScore = 0.3f` — hardcoded but expected for emergency fallback path. L141-L146: `ConfidenceBreakdown` with hardcoded contributions (0.1f/0.1f/0f). `finalScore = overallConfidence` at L146 — value equals 0.3f from input. Falls within the "derived signal" invariant since emergency path explicitly signals low confidence. |
| Concurrency | **VIOLATION** | L183-L184: `private var titleMethod = "NONE"` and `private var companyMethod = "NONE"` are **module-level mutable vars** — shared across all threads. Multiple concurrent `extractJob()` calls will race on these variables, producing non-deterministic method-reporting behavior (though method names are only used for assignment, not flow control — no observable correctness impact on output, but violates module-level state safety). |
| 1.8 | PRESENT | L45: `PipelineTrace.warn` for emergency invocation. L172-L178: `ExtractionValidator.validateJob` with PipelineTrace warning on validation failure. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **ABSENT** | All iterations are over deterministic List structures |
| unused | | |

---

#### 11. DocumentPreprocessor.kt

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/DocumentPreprocessor.kt`  
**Role:** Normalizes raw document text before extraction pipeline.

| Property | Observation |
|---|---|
| Symbols | `DocumentPreprocessor` (object), `PreprocessingResult` (data class) |
| Entry points | `preprocess(rawText, correlationId)` |
| Dependencies | `PipelineTrace`, `Regex` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | L22-L34: Unicode normalization replacements — ordered sequence. L37: `repairHyphenation` — deterministic regex. L39: `repairOcrConfusionPairs` — ordered regex replacements. L41-L45: Whitespace normalization — deterministic. L80-L148: `estimateQuality` — deterministic computation from text metrics. L20: `mutableListOf<String>()` for warnings — insertion-order. |
| 1.5 | OBSERVED | `estimateQuality` (L80-L148) — heuristic computation based on character ratios, word lengths, gibberish patterns. Hardcoded thresholds and penalty values (L111-L141). Score `coerceIn(0.0f, 1.0f)` at L147. Not a confidence flow-control point — quality score is informational metadata. |
| 1.8 | PRESENT | L16-L19: `PipelineTrace.entry` with textLength, correlationId. L50-L54: `PipelineTrace.exit` with quality, warnings. |

**Presence of Risk Indicators:**

| Risk Indicator | Status |
|---|---|
| unordered iteration | **ABSENT** |

---

#### 12. JobExtractionOrchestrator.kt

**Path:** `app/src/main/java/com/vantedge/app/domain/extraction/JobExtractionOrchestrator.kt`  
**Role:** Orchestrates job extraction — preprocess → structure analysis → LLM extraction → validation → confidence → decision.

| Property | Observation |
|---|---|
| Symbols | `JobExtractionOrchestrator` (class) |
| Entry points | `extractJob(rawText, sourceType, onProgress)` |
| Dependencies | `DocumentPreprocessor`, `DocumentStructureAnalyzer`, `LlmJobExtractor`, `SchemaValidator`, `SemanticValidator`, `ConfidenceEngine`, `ExtractionDecisionCoordinator`, `RuleBasedEmergencyExtractor` |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | OBSERVED | Sequential deterministic pipeline. No unordered iteration sources. L69: `SchemaValidator.validate(llmResult.rawJson, ...)` — deterministic. L72: `SemanticValidator.validate(...)` — deterministic. L80: `ConfidenceEngine.compute(...)` — deterministic. L88: `ExtractionDecisionCoordinator.decide(...)` — deterministic. |
| 1.2 | PRESENT | L69: Schema validation enforced with `schemaFailures` passed to `ConfidenceEngine`. L72: Semantic validation enforced. Schema/Semantic failures propagate to decision layer. |
| 1.5 | OBSERVED | L173-L183: `buildJobExtractionResult()` — hardcoded contribution weights: L173-L174: `0.15f` for title/company contributions; L177: `overallConfidence * 0.6f` for base score. `overallConfidence` sourced from `ConfidenceEngine.compute()` (not hardcoded). `finalScore = overallConfidence` at L182. L191: `confidenceScore = overallConfidence`. |
| 1.8 | PRESENT | L41-L45: `PipelineTrace.entry`. L97-L108: Decision-status `PipelineTrace.warn/error`. L127-L134: `PipelineTrace.exit` for LLM path. L141-L146: `PipelineTrace.exit` for emergency path. L151: `PipelineTrace.error` on failure. L154: `PipelineTrace.warn` for emergency fallback. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| hardcoded confidence | **PRESENT** | L173-L174: 0.15f, L177: 0.6f |

---

### AI GATEWAY / REQUEST LAYER (Controlled Observation Only)

*Per §2.3: Only inference parameters, response parsing boundaries, and token extraction logic were inspected.*

---

#### 13. AiGateway.kt

**Path:** `app/src/main/java/com/vantedge/app/data/network/AiGateway.kt`  
**Role:** Gateway for AI model calls — single `generate(pipeline, request, budgetMs)` signature.

| Check | Observation |
|---|---|
| Inference parameters logged | L29-L36: `PipelineTrace.entry` includes `temperature`, `maxTokens`. |
| `classifyFailure` mapping | L149-L161: Maps raw string error types to `AiFailureCategory`. Uses legacy raw strings ("timeout", "cancelled", "network", "empty", "invalid_json", "provider_error", "schema_error", "parse", "404", "429"). Not using `RequestFailureType` enum from W3-D (noted — W3 frozen, not actionable). |
| Response parsing | No parsing at gateway level — returns `String?` directly from `GeminiService.generate()`. |

---

#### 14. GeminiService.kt — Inference Parameters Only

**Path:** `app/src/main/java/com/vantedge/app/data/network/GeminiService.kt` (lines 449-451)  
**Role:** AI service layer — transmits inference parameters to OpenRouter API.

| Parameter | Value | Source |
|---|---|---|
| `max_tokens` | `aiRequest.maxTokens` | Configurable via AiRequest (default 8192) |
| `temperature` | `aiRequest.temperature` | Configurable via AiRequest (default 0.2) |
| `top_p` | `0.8` (hardcoded) | Fixed — never overridden |

| Invariant | Status | Evidence |
|---|---|---|
| 1.3 | OBSERVED | L450: Default temperature 0.2 — within deterministic range. Analytical pipeline callers: `LlmJobExtractor` hardcodes 0.1 (L89), `OptimizationOrchestrator` uses default 0.2. Generative pipelines (CV/cover letter — excluded per §1.3) may use configurable temperature. `top_p=0.8` hardcoded at L451 — deterministic but limits model nucleus sampling to 80% probability mass. |

---

#### 15. AiRequest.kt

**Path:** `app/src/main/java/com/vantedge/app/data/network/AiRequest.kt`  
**Role:** AI request payload data class.

| Field | Default | Configurable |
|---|---|---|
| `systemPrompt: String` | — | Yes |
| `userPrompt: String` | — | Yes |
| `temperature: Double` | 0.2 | Yes |
| `maxTokens: Int` | 8192 | Yes |

| Invariant | Status |
|---|---|
| 1.3 | All analytical pipeline callers use ≤0.2 temperature. |

---

#### 16. LlmJobExtractor.kt — Response Parsing Only

**Path:** `app/src/main/java/com/vantedge/app/data/engine/extraction/LlmJobExtractor.kt`  
**Role:** LLM-based job field extraction with schema validation and parsing.

| Property | Observation |
|---|---|
| Response parsing | L99: `JsonExtractionEngine.extract(response)`. L114: `parseWithRepairFallback(extractionResult.content)`. L130: `parseResponse(jsonObj)`. |
| Inference params | L89: `temperature = 0.1` (hardcoded override), L90: `maxTokens = 4096` (hardcoded override) |

**Invariant Conformance:**

| Invariant | Status | Evidence |
|---|---|---|
| 1.1 | **VIOLATION** | L252: `val confidence = mutableMapOf<String, Float>()` — insertion-order dependent map. L133: `result.fieldConfidence.entries.joinToString(", ")` — entry ordering depends on map iteration order. |
| 1.2 | PRESENT | L116: `SchemaValidator.validate(jsonObj.toString(), ExtractionMethod.LLM, correlationId)` — schema validation enforced before response parsing. Schema failure throws `SCHEMA_VIOLATION` at L127. |
| 1.4 | **GAP** | Inherits from `JsonExtractionEngine` (L99). Only markdown fences stripped. No guard against non-markdown reasoning artifacts. `parseWithRepairFallback` (L206-L248) uses `JSONObject` directly or `JsonRepairUtil.repair` — neither handles CoT/xml/text prefix/suffix contamination. |

**Presence of Risk Indicators:**

| Risk Indicator | Status | Location |
|---|---|---|
| unordered iteration | **PRESENT** | L252 `mutableMapOf`, L133 `entries.joinToString` |
| reasoning token leakage | **GAP** | Inherited from JsonExtractionEngine at L99 |

---

### COMPATIBILITY DOMAIN

---

#### 17. CompatibilityStore.kt

**Path:** `app/src/main/java/com/vantedge/app/data/storage/CompatibilityStore.kt`  
**Role:** In-memory persistence layer for compatibility records.

| Property | Observation |
|---|---|
| Symbols | `CompatibilityStore` (class) |
| Fields | `_historyFlow: MutableStateFlow<List<CompatibilityRecord>>`, `mutex: Mutex` |
| Methods | `addRecord`, `deleteRecord`, `clearAll` |

| Check | Observation |
|---|---|
| Concurrency | `mutex.withLock` at L17 for all writes. Single-threaded access per caller due to mutual exclusion. |
| Persistence contract | In-memory only — no Room/serialization. Data is lost on process death. |
| Recorded for completeness | Yes — no assumptions about usage patterns beyond code surface. |

---

## Violations Summary by W4 v2.1 Invariant

| Invariant | Files | Description | Severity |
|---|---|---|---|
| **1.1** — Unordered iteration | `ConfidenceEngine.kt` (L31/L29/L60), `SemanticValidator.kt` (L48/L102), `LlmJobExtractor.kt` (L252/L133) | `toSet()`, `mutableMapOf`, `entries.joinToString` produce non-deterministic entry ordering across confidence computation and logging | Consistent |
| **1.2** — Schema integrity bypass | `CompatibilityEngine.kt` (L39-L128) | No schema pre-validation. `JsonNode.path()` silently returns defaults for missing fields. Malformed AI output produces zero-value `CompatibilityRecord` without error | Single file |
| **1.4** — Reasoning token containment | `JsonExtractionEngine.kt` (L53-L57), `LlmJobExtractor.kt` (L99) | Only markdown fences stripped. No guard against chain-of-thought, XML reasoning tags, or unstructured prefix/suffix text preceding/following JSON output | **Architectural gap** (2 files) |
| **1.5** — Hardcoded confidence | `ProfileExtractionEngine.kt` (L652-L733) | All 17 `ExtractedField` construction sites use 0.8f. Confidence entirely decoupled from AI model quality. ConfidenceEngine never called for profile extraction path | Single file — severe |
| **1.7** — Unvalidated external references | `CompatibilityEngine.kt` (L86-L91), `CourseRecommendation.kt` (model) | `url`/`provider` ingested from AI output without verification. No `isVerified`/`verificationStatus` property on `CourseRecommendation` model | Single file + model |

## Additional Findings

| Finding | File | Detail |
|---|---|---|
| Concurrency hazard | `RuleBasedEmergencyExtractor.kt` (L183-L184) | Module-level mutable vars `titleMethod`/`companyMethod` shared across concurrent calls — assignment race |
| `classifyFailure` raw strings | `AiGateway.kt` (L149-L161) | Maps legacy raw strings, not `RequestFailureType` enum (W3-frozen domain — observed, not actionable) |
| Observability gap | `CompatibilityEngine.kt` | No `PipelineTrace` calls — observability enters only through upstream `ContractValidator` |
| Observability gap | `ProfileExtractionEngine.parse()` | No `PipelineTrace` calls — observability exists upstream in `callAi()`/`structureProfile()` |

---

## Presence/Absence Matrix of Risk Indicators

| File | unordered iteration | hardcoded confidence | unvalidated URLs | reasoning leakage | schema mismatch |
|---|---|---|---|---|---|
| ConfidenceEngine.kt | **PRESENT** | PRESENT | — | — | — |
| ExtractionDecisionCoordinator.kt | **PRESENT** | PRESENT | — | — | — |
| JsonExtractionEngine.kt | ABSENT | — | — | **GAP** | — |
| SemanticValidator.kt | **PRESENT** | PRESENT | — | — | — |
| CompatibilityEngine.kt | ABSENT | — | **PRESENT** | — | **PRESENT** |
| CompatibilityResult.kt | — | — | *propagated* | — | — |
| JsonRepairUtil.kt | ABSENT | — | — | *no guard* | — |
| PipelineTrace.kt | AMBIGUOUS | — | — | — | — |
| ProfileExtractionEngine.parse() | ABSENT | **PRESENT** (17 sites) | — | — | — |
| RuleBasedEmergencyExtractor.kt | ABSENT | PRESENT (expected) | — | — | — |
| DocumentPreprocessor.kt | ABSENT | — | — | — | — |
| JobExtractionOrchestrator.kt | ABSENT | PRESENT | — | — | PRESENT |
| LlmJobExtractor.kt | **PRESENT** | — | — | **GAP** (inherited) | PRESENT |

Key: **PRESENT** = invariant violation confirmed | *italic* = observed but qualified | ABSENT = not found | GAP = missing architectural mechanism

---

## End of Report

**No conclusions. No recommendations. No proposals. No fixes.**  
This report presents codebase-truth observations only, mapped to W4 v2.1 execution contract invariants per §1 and §3 exclusion boundaries.
